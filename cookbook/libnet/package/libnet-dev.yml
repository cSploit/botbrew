Package: libnet-dev
Version: 1.1.6-0
Architecture: armeabi
Section: libdevel
Priority: optional
Source: http://libnet-dev.sourceforge.net/
Description: |
 library for the construction and handling of network packets
Depends: libnet
Data:
 /CONTROL/init: |
    #!/bin/sh
    mkdir -p ${PACKAGE}${PREFIX}/lib
    cp -r ${EXPORT}${PREFIX}/include ${EXPORT}${PREFIX}/
    cp -r ${EXPORT}${PREFIX}/lib/*.a ${EXPORT}${PREFIX}/lib/*.la ${PACKAGE}${PREFIX}/lib/
